<div class="col text-center">
  <%= link_to t_action(:edit), edit_location_path(@location) %> |
  <%= link_to t_action(:back), locations_path %> |
  <%= link_to t_action(:delete), @location, method: :delete, data: { confirm: "你確定要刪除此項資料嗎?" },  class: "text-danger" %>
</div>

<div class="row mb-2">
  <div class="col-md-1"><strong><%= t_attr("name") %></strong></div>
  <div class="col"><%= @location.name %></div>
</div>